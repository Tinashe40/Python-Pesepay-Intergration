[phases.build]
commands = [
  "pip install -r requirements.txt",
  "python manage.py collectstatic --noinput"
]

[phases.start]
commands = [
  "gunicorn donations.wsgi:application --bind 0.0.0.0:$PORT"
]
