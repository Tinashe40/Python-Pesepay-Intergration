[phases]
  [phases.build]
  commands = [
    "pip install --no-cache-dir -r requirements.txt"
  ]

  [phases.start]
  commands = [
    "gunicorn donations.wsgi:application --bind 0.0.0.0:$PORT --workers 3"
  ]

  [phases.release]
  commands = [
    "python manage.py migrate"
  ]
